<%= form_with model:[book, book_comment], local: false do |f| %>
  <%= f.text_area :comment, row:'5', plaseholder: "コメントをここに", class: "w-100" %><br>
  <%= f.submit "送信する", class: "btn btn-base-1 mt-20 pull-right btn-outline-secondary" %>
<% end %>
