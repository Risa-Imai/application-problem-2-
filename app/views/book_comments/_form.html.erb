<%= form_with model: [book, book_comment] do |f| %>
  <%= f.text_area :comment, row:'5', plaseholder: "コメントをここに", class: "w-100" %><br>
  <%= f.submit "送信", class: "btnn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>